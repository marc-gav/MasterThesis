project: Probing classifier - Simple 2 layer NN
method: bayes
metric:
  name: validation_loss
  goal: minimize
parameters:
  learning_rate:
    min: 0.001
    max: 0.1
  optimizer:
    values: ["adam"]
  batch_size:
    values: [32, 64]